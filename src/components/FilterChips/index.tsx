import React, { ReactElement } from 'react';

export interface FilterChipsProps {
    data: null;
}

const FilterChips = (props: FilterChipsProps): ReactElement => {
    const {} = props;

    return <React.Fragment></React.Fragment>;
};

export default FilterChips;
