import React, { ReactElement } from "react";
import AppNav from "../../../../components/AppNav";

const Conversions = (): ReactElement => {
  return <AppNav></AppNav>;
};

export default Conversions;
